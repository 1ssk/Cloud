# Cloud Service

Это облачный сервис, который позволяет загружать и хранить файлы. Приложение состоит из сервера на Go, базы данных PostgreSQL и фронтенда на HTML, CSS и JavaScript.

## Структура проекта
- `cmd/server/main.go` — главный файл для запуска сервера.
- `internal/` — внутренний код, включая работу с базой данных и обработку запросов.
- `static/` — фронтенд (HTML, CSS, JS).
- `uploads/` — папка для хранения загруженных файлов.

## Запуск
1. Установите зависимости:
   ```bash
   go mod tidy
